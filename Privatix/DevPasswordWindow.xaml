<Window x:Class="Privatix.DevPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Privatix" Height="145" Width="385" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Margin="26,23,5,5" Foreground="Black" FontSize="15" FontFamily="Segoe UI"
                   IsHyphenationEnabled="True" LineStackingStrategy="BlockLineHeight" LineHeight="15"
                   TextAlignment="Left" TextWrapping="Wrap" Text="Enter password"/>
        <PasswordBox x:Name="pbPassword" Grid.Row="1" Grid.ColumnSpan="2" Margin="5,5,5,5" Width="336" Height="20" />
        <Button x:Name="btnOk" Grid.Row="2" Grid.Column="0" Content="OK" HorizontalAlignment="Right" Margin="5,5,5,5" VerticalAlignment="Top" Width="75" 
                IsDefault="True" Click="btnOk_Click" />
        <Button x:Name="btnCancel" Grid.Row="2" Grid.Column="1" Content="Cancel" HorizontalAlignment="Right" Margin="5,5,21,5" VerticalAlignment="Top" 
                Width="75" Click="btnCancel_Click"/>
    </Grid>
</Window>
